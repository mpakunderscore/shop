let items = [
    'batteries today',
    'batteries now',
    'batteries order',
    'batteries power',
    'batteries forever',
    'batteries love',
    'batteries shop',
    'batteries future',
    'batteries live',
    'batteries justice',
    'batteries energy',
    'batteries electricity'
];

(function move() {

    let title = document.getElementById("title");

    // title.innerText = items[Math.floor(Math.random() * items.length)];

    // setTimeout(move, Math.floor(Math.random() * 10000));
})();